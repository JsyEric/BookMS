<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.jsy.dao.RecordInt">
        <select id="getByBookId" resultType="com.jsy.bean.Record">
            select * from record where bookId = #{id}
        </select>
</mapper>